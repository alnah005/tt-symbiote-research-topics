# B Review — Pass 1

1. **`section_dimension_assignment.md`, ~line 179 (code comment)** — The code block declares `cos_table` as shape `[max_seq_len, rotary_dim]` (i.e., the 64-wide doubled table), but the slice indices `[:s_t]`, `[s_t:s_t+s_h]`, `[s_t+s_h:]` treat it as the half-length table of width `rotary_dim/2 = 32`. If an implementer supplies the 64-wide doubled table as written in the comment, they get only 32 of the 64 columns per gather (discarding the second copy) and then double to 64 — producing a wrong assembled vector. Fix: change the comment from `cos_table: [max_seq_len, rotary_dim]` to `cos_table: [max_seq_len, rotary_dim//2]` (i.e., `[T, 32]`), consistent with the inv_freq-based half-length table used in the HuggingFace reference.

2. **`mrope_motivation_and_design.md`, lines 102–107 vs. `section_dimension_assignment.md`, lines 184–186** — The two files use incompatible slice widths for the same operation. `mrope_motivation_and_design.md` slices the doubled cos_table as `[0:2*s_t]`, `[2*s_t:2*(s_t+s_h)]`, `[2*(s_t+s_h):rotary_dim]` (widths 22, 22, 20). `section_dimension_assignment.md` slices as `[:s_t]`, `[s_t:s_t+s_h]`, `[s_t+s_h:]` (widths 11, 11, 10). Both cannot be correct simultaneously. The HuggingFace reference assembles from the half-length table (widths 11, 11, 10), so the formula in `mrope_motivation_and_design.md` is wrong. Fix: change the assembled-cos formula in `mrope_motivation_and_design.md` to slice widths `s_t`, `s_h`, `s_w` (not `2*s_t`, `2*s_h`, `2*s_w`) and label the table as the half-length frequency table of width `rotary_dim/2`.

3. **`section_dimension_assignment.md`, lines 138–140** — The text claims the three sections occupy contiguous column ranges `[0, 2*s_t)`, `[2*s_t, 2*(s_t+s_h))`, `[2*(s_t+s_h), rotary_dim)` in the assembled cos/sin vector. For Qwen3.6 this would mean temporal=`[0,22)`, height=`[22,44)`, width=`[44,64)`. But the code block at lines 97–102 of the same file (the authoritative HuggingFace convention) shows the assembled layout as `[t₀…t₁₀, h₁₁…h₂₁, w₂₂…w₃₁, t₀…t₁₀, h₁₁…h₂₁, w₂₂…w₃₁]`, placing temporal in `[0,11)` and `[32,43)`, height in `[11,22)` and `[43,54)`, width in `[22,32)` and `[54,64)`. The paragraph's contiguous-range claim (`[0,22)` for temporal, etc.) describes a different (non-HuggingFace) double-table convention and contradicts the code block in the same file. An implementer reading the prose would build a different vector layout than the code produces. Fix: replace the three column ranges with `[0, s_t)` and `[rotary_dim/2, rotary_dim/2+s_t)` for temporal, and analogously for height and width — or simply delete the range claim and refer readers to the code block directly.

4. **`standard_rope_recap.md`, line 49** — The comment `# inv_freq[0] = 1.0  (highest frequency — rotates one full cycle every 1/1 token)` is numerically wrong about the period. `inv_freq[0] = θ_0 = 1.0` is an angular frequency in radians per token. One full rotation cycle requires `2π ≈ 6.28` tokens, not 1 token. Stating the period as "1 token" will lead an implementer to misunderstand what "highest frequency" means quantitatively and could cause errors in any context-length reasoning based on this number. Fix: change the comment to `# inv_freq[0] = 1.0 rad/token  (highest frequency; period ≈ 2π ≈ 6.28 tokens)`.

5. **`section_dimension_assignment.md`, lines 81–82** — The table row for the Height section first-half columns states `[22, 44)` and then the second-half column arithmetic is flagged with `... wait — 54+22=76 > 64`. The file correctly identifies the overflow and then resolves it in the subsequent paragraphs by switching to the HuggingFace half-table convention. However, the partially-constructed (and numerically overflowing) table row is never corrected or removed — it remains in the rendered document with no final correct value for the height second-half range. A reader may record `[54, 76)` as the answer. Fix: either complete the table with correct HuggingFace-convention ranges (as given later in the full dimension map on lines 115–119), or remove the incomplete table and replace it with a forward pointer to the corrected table.

# B Review — Pass 2

All five Pass 1 items have been resolved. Two new correctness issues remain.

1. **`mrope_motivation_and_design.md`, lines 131–133 (Qwen3.6 section table, "Real dimension range" column)** — The table lists real dimension ranges as `[0, 22)`, `[22, 44)`, `[44, 64)` — contiguous blocks of width `2*s`. These numbers correspond to the adjacent-pair convention (pair `i` occupies real dimensions `2i` and `2i+1`). However, the entire chapter uses the HuggingFace rotate-half convention, under which pair `i` occupies column `i` and column `i + rotary_dim/2`. Under that convention the correct real dimension ranges are `[0,11)∪[32,43)` for temporal, `[11,22)∪[43,54)` for height, and `[22,32)∪[54,64)` for width — exactly as `section_dimension_assignment.md` lines 129–131 states. An implementer relying on this table would apply the wrong slices to the assembled cos/sin vector. Fix: replace the three "Real dimension range" values with the HuggingFace-convention non-contiguous ranges (or add a note that these are adjacent-pair convention ranges and are not the actual column layout in the assembled vector).

2. **`mrope_motivation_and_design.md`, line 197 (degenerate-text-RoPE code block)** — The inline code block slices `cos_table[t]` using indices `0:2*s_t`, `2*s_t:2*(s_t+s_h)`, and `2*(s_t+s_h):rotary_dim`, treating `cos_table` as the full doubled table of width `rotary_dim`. But lines 102–110 of the same file establish that `cos_table` is the half-length frequency table of width `rotary_dim//2`, and the gather step uses widths `s_t`, `s_h`, `s_w` (not `2*s_t`, etc.). These two representations are internally inconsistent. An implementer following this block would write `cos_table[t][0:22]` on a width-32 table (which does not raise an error but returns the wrong slice) instead of `cos_table[t][0:32]`. Fix: change the slice indices to `0:s_t`, `s_t:s_t+s_h`, `s_t+s_h:rotary_dim//2` and the final result to `cos_table[t][0:rotary_dim//2]`, consistent with the half-table convention used in the rest of the file.

# B Review — Pass 3

1. **`standard_rope_recap.md`, line 50 (inv_freq[31] numerical value)** — The comment states `inv_freq[31] ≈ 1.74e-6`, but the correct value is approximately `1.54e-6`. The formula is `1 / (1e6)^(62/64)`. Computing: `(1e6)^(62/64) = 10^(6 × 62/64) = 10^5.8125 ≈ 649,400`, giving `inv_freq[31] ≈ 1.54e-6`. The stated value of `1.74e-6` is about 13% too high. An implementer using this comment to calibrate a numerical sanity check would accept a wrong value. Fix: change the comment to `# inv_freq[31] = 1.0 / (1e6 ** (62/64)) ≈ 1.54e-6  (very slow rotation)`.

No further correctness issues found. All Pass 1 and Pass 2 items are confirmed resolved. Navigation footers, clickable links, and LaTeX equation formatting are all correct.

# B Review — Pass 4

1. **`standard_rope_recap.md`, line 132** — The sentence "This is algebraically equivalent to applying the 2×2 rotation matrix to each pair `(x_{2i}, x_{2i+1})`" is factually wrong. The rotate-half operation pairs dimension `i` with dimension `i + rotary_dim/2`, not dimension `2i` with `2i+1`. The adjacent-pair convention `(x_{2i}, x_{2i+1})` is a distinct implementation from rotate-half and produces numerically different Q/K tensors. The text on lines 134–135 partially acknowledges the inconsistency with "...but wait..." but never retracts the false claim on line 132; the premise of the sentence remains incorrect. A reader implementing from line 132 would use adjacent-pair indexing and get silent numerical errors. Fix: change line 132 to "This is algebraically equivalent to applying the 2×2 rotation matrix to each pair `(x_i, x_{i + rotary_dim/2})`:" — which is the correct statement of what rotate-half computes — then remove or rephrase the "...but wait..." clause accordingly.

# B Review — Pass 5

1. **`index.md`, line 71 (Key Terminology table, "rotate-half" row)** — The definition states the rotate-half apply operation as `(x_{2i}, x_{2i+1}) → (x_{2i}·cos − x_{2i+1}·sin, x_{2i}·sin + x_{2i+1}·cos)`. This is the adjacent-pair convention, which is factually wrong for the rotate-half operation used throughout this guide. The rotate-half operation pairs dimension `i` with dimension `i + rotary_dim/2` (not `2i` with `2i+1`), producing output at position `i`: `x_i·cos − x_{i+rotary_dim/2}·sin`, and at position `i + rotary_dim/2`: `x_i·sin + x_{i+rotary_dim/2}·cos`. Pass 4 corrected `standard_rope_recap.md` line 132 to use the right pairing, but the terminology table in `index.md` — which is a standalone reference definition — still carries the incorrect adjacent-pair formula. A reader using this table as a quick reference to implement the operation would produce wrong Q/K values. Fix: replace the "rotate-half" definition with `x_i` paired with `x_{i+rotary\_dim/2}`, e.g.: `pair i: (x_i, x_{i+rotary\_dim/2}) → (x_i·cos − x_{i+rotary\_dim/2}·sin, x_i·sin + x_{i+rotary\_dim/2}·cos)`.

# B Review — Pass 6

**No feedback — chapter approved.**

# B Review — Pass 7

1. **`standard_rope_recap.md`, line 111 ("Algebraic Basis" section)** — The sentence "RoPE applies this independently to each dimension pair `(x_{2i}, x_{2i+1})` of a query or key vector" uses the adjacent-pair convention, which is factually wrong for this guide's rotate-half implementation. The rotate-half operation pairs dimension `i` with dimension `i + rotary_dim/2`, not `2i` with `2i+1`. Pass 4 corrected the analogous statement at line 132 ("This is algebraically equivalent to applying the 2×2 rotation matrix to each pair `(x_i, x_{i + rotary_dim/2})`"), but line 111 — which is the first place in the file where the pairing is stated — still carries the incorrect adjacent-pair notation. A reader who stops at line 111 before reaching the corrected line 132 will implement the wrong pairing. Fix: change `(x_{2i}, x_{2i+1})` on line 111 to `(x_i, x_{i + rotary\_dim/2})`.

No further issues found. Compression edits to `mrope_motivation_and_design.md` and `section_dimension_assignment.md` are coherent: the apply-step description in `mrope_motivation_and_design.md` is self-consistent after the math block removal (the forward reference to `section_dimension_assignment.md` is accurate and the degenerate-text-RoPE assembly uses the correct half-table slices). The removed 3-bullet column-range list in `section_dimension_assignment.md` leaves no gap — the full dimension map table and the code block provide complete coverage. Navigation footers, clickable links, Prerequisites section, Reading Order references, and `math` block formatting are all correct.

# B Review — Pass 8

1. **`index.md`, lines 76 and 80 (Forward References section) — non-clickable cross-chapter links.** The Chapter 2 and Chapter 3 references are written as plain backtick-quoted paths inside parentheses: `` **Chapter 2** (`../ch2_qwen36_mrope_config/index.md`) `` and `` **Chapter 3** (`../ch3_text_only_reduction/index.md`) ``. Neither is formatted as a `[text](./path)` markdown link. By contrast, the inline references to `mrope_motivation_and_design.md` and `section_dimension_assignment.md` on lines 82–84 of the same section are properly formatted as clickable links. Fix: replace each plain-text path with a markdown link, e.g. `[Chapter 2 — M-RoPE in Qwen3.6-35B-A3B](../ch2_qwen36_mrope_config/index.md)` and `[Chapter 3 — Text-Only Reduction](../ch3_text_only_reduction/index.md)`.

Pass 7 fix verified: `standard_rope_recap.md` line 110–111 now correctly states `(x_i, x_{i + rotary_dim/2})` (rotate-half convention). All other items from Passes 1–7 remain resolved. No further factual errors, coherence gaps, structural gaps, display-equation formatting issues, or non-clickable links found beyond item 1 above.

# B Review — Pass 9

Pass 8 fix verified: `index.md` Forward References section (lines 76 and 80) now correctly renders `[Chapter 2 — M-RoPE in Qwen3.6-35B-A3B](../ch2_qwen36_mrope_config/index.md)` and `[Chapter 3 — Text-Only Reduction](../ch3_text_only_reduction/index.md)` as proper markdown links. Fix confirmed.

1. **`section_dimension_assignment.md`, "Cross-Reference" section (lines 208–221) — non-clickable cross-chapter links.** The two inter-chapter references are written as plain backtick-quoted paths: `Chapter 4 (\`../ch4_ttnn_implementation/extension_approach.md\`)` and `Chapter 2 (\`../ch2_qwen36_mrope_config/hf_reference_implementation.md\`)`. Neither is formatted as a `[text](./path)` markdown link. This is the same pattern that was flagged and fixed in `index.md` in Pass 8, but the same issue persists in this content file. Fix: replace each plain-text path with a markdown link, e.g. `[Chapter 4 — TTNN Implementation](../ch4_ttnn_implementation/extension_approach.md)` and `[Chapter 2 — HuggingFace Reference Implementation](../ch2_qwen36_mrope_config/hf_reference_implementation.md)`.

# B Review — Pass 10

Pass 9 fix verified: `section_dimension_assignment.md` Cross-Reference section (lines 208 and 218) now correctly renders `[Chapter 4 — TTNN Implementation](../ch4_ttnn_implementation/extension_approach.md)` and `[Chapter 2 — HuggingFace Reference](../ch2_qwen36_mrope_config/hf_reference_implementation.md)` as proper markdown links. Fix confirmed.

No feedback — chapter approved.
