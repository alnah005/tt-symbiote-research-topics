# B Review — Final Pass — Cross-Chapter Consistency — Pass 1

1. **`FABRIC_1D` vs `FABRIC_1D_RING` — contradictory fabric config requirement across Ch3 and Ch4 (implementer will use wrong enum constant).**
   Ch3 `q3_requirements_and_limitations.md` Requirement 3 states: "Action: Confirm that `device_params` … sets `"fabric_config": ttnn.FabricConfig.FABRIC_1D` for T3K runs." The same value `FABRIC_1D` appears in `q1_trace_compatibility.md` (table row "Fabric config on T3K") and in the Ch3 `index.md` Q3 one-line answer. Ch4 `integration_checklist.md` explicitly contradicts this: "The ring topology required for T3K cluster axis 1 is `FABRIC_1D_RING`" and includes a Warning callout: "`ttnn.FabricConfig.FABRIC_1D` and `ttnn.FabricConfig.FABRIC_1D_RING` are distinct enum values." A reader following Ch3's actionable instruction would set the wrong fabric constant and the ring collective would fail or silently misbehave. One chapter is factually wrong; Ch3 must be brought into alignment with Ch4's `FABRIC_1D_RING`.

2. **Composite path mislabelled as "all-gather + `moreh_sum`" in Ch2 index — omits the `all_broadcast` + `concat` stage, causing a reader to misidentify the path in source code.**
   Ch2 `index.md` Summary box (line 23) and reading-order entry (line 55) describe the composite path as "all-gather + `moreh_sum`". Every substantive content file in Ch2 and Ch3 (including `composite_path.md` §1–4 and `q3_requirements_and_limitations.md` Limitation 1) correctly identifies the composite path as `composite_all_gather` (= `ttnn::prim::all_broadcast` + `ttnn::concat`) followed by a local reduce (`moreh_sum` or `ttnn::transpose` + `ttnn::sum`). The Ch2 index shorthand collapses the first stage entirely, leaving only the second stage visible. A reader who uses the Ch2 index label to locate the composite branch in `all_reduce_async.cpp` will search for an `all_gather` call and fail to find it, because the actual first-stage function called is `composite_all_gather` (a different symbol), not `ttnn::all_gather`. This causes incorrect implementation identification. The Ch2 index labels should read "composite path (`composite_all_gather` + local reduce) — why it is trace-incompatible" to match the content.

3. **Ch4 cross-reference sends readers to "Chapter 2 (`q2_semaphore_state.md`)" but `q2_semaphore_state.md` is a Ch3 file.**
   `integration_checklist.md` line 207 reads: "See Chapter 2 (`q2_semaphore_state.md`) for the semaphore audit." The file `q2_semaphore_state.md` lives under `ch3_verdict/`, not `ch2_all_reduce_internals/`. The correct reference is Chapter 3 (`q2_semaphore_state.md`). A reader following this cross-reference will look in Ch2 for a file that does not exist there, or will navigate to Ch2's content files and find no semaphore audit file, undermining trust in the navigation structure.

4. **"Capture-window buffer" and "warm-up buffer" are load-bearing terms introduced in Ch4 with no forward-reference or definition in Ch1–Ch3.**
   Ch1 `index.md` (line 23) defines two buffer categories: "persistent input buffers" and "persistent output buffers." Ch1 `buffer_address_stability.md` elaborates these as the only named buffer categories. Ch4 `integration_checklist.md` (lines 131–136) and `minimal_test_pattern.md` (lines 183–211, 290–295) introduce two additional named categories — "capture-window output buffer" (`capture_output`) and "warm-up buffer" (`TraceEntry.trace_output`) — and explain that `ttnn.execute_trace` writes into the capture-window buffer but `TracedRun.module_run` returns the warm-up buffer (a known limitation). This distinction is the reason why reading `entry.trace_output` after replay returns stale data. No chapter before Ch4 mentions either term or forward-references this distinction. A reader who absorbs Ch1–Ch3 and then uses `TracedRun.module_run`'s return value to verify replay correctness will silently read stale warm-up data and conclude the trace is correct when it may not be. The distinction should be introduced (even briefly) in the Ch1 persistent-output-buffer section or in the Ch3 requirements section, with a forward reference to Ch4 for the full treatment.

# B Review — Final Pass — Cross-Chapter Consistency — Pass 2

1. **Ch3 `index.md` Q3 one-line answer names `FABRIC_1D` instead of `FABRIC_1D_RING`, causing an implementer reading only the chapter summary to configure the wrong fabric constant.**
   Ch3 `index.md` line 15 (Q3 one-line answer in the research-questions table) lists the five requirements as: "output buffer stability, intermediate buffer pre-allocation, `FABRIC_1D` fabric config, even ring size, and confirmed routing to the non-composite path." The fabric config requirement is stated as `FABRIC_1D`. The substantive file `q3_requirements_and_limitations.md` Requirement 3 correctly states `FABRIC_1D_RING` and includes a Warning callout explaining that `FABRIC_1D` (value 1) and `FABRIC_1D_RING` (value 2) are distinct enum values with different routing behavior. Ch4 `integration_checklist.md` also consistently requires `FABRIC_1D_RING`. The Ch3 index summary is the highest-visibility entry point for the requirements list; a reader who uses it as a checklist — without reading Requirement 3 in full — will set `FABRIC_1D` and the ring collective will route incorrectly on T3K. The Ch3 `index.md` Q3 one-line answer must be corrected to `FABRIC_1D_RING`.

# B Review — Final Pass — Pass 3

1. **`q1_trace_compatibility.md` table and Key Finding callout still say `FABRIC_1D` for the T3K fabric config, contradicting `q3_requirements_and_limitations.md` Requirement 3 and Ch4 — implementer using q1's table as a checklist will configure the wrong enum constant.**
   `q1_trace_compatibility.md` line 128 (T3K path-assessment table) lists "Fabric config on T3K | `FABRIC_1D`" and the Key Finding callout immediately below (lines 140–143) repeats "On T3K with `FABRIC_1D` and standard hidden dimensions…". Both references use the wrong constant. `q3_requirements_and_limitations.md` Requirement 3 (correctly updated) states `FABRIC_1D_RING` and its Warning callout explicitly explains that `FABRIC_1D` (value 1) and `FABRIC_1D_RING` (value 2) are distinct enum values with different routing behavior; `FABRIC_1D` does not apply deadlock-avoidance datelines required for the ring topology. Ch4 `integration_checklist.md` also requires `FABRIC_1D_RING`. A reader who uses q1's table to verify their device setup — a natural use of a path-assessment table — will configure `FABRIC_1D` and the ring collective will route incorrectly on T3K. The two occurrences in `q1_trace_compatibility.md` must be corrected to `FABRIC_1D_RING`.

# B Review — Final Pass — Pass 4

1. **`q1_trace_compatibility.md` "Evidence from the existing codebase" section presents `ttnn.experimental.all_reduce_async` tests as validation of `ttnn.all_reduce` trace compatibility — an implementer will incorrectly conclude the synchronous non-async path has tested trace coverage.**
   Lines 110–150 of `q1_trace_compatibility.md` carry the heading "Evidence from the existing codebase" and describe two tests as demonstrating that "CCL-class operations can be traced when the buffer stability requirement is met." Both tests (`test_new_all_reduce.py` and `test_ccl_all_reduce.py`) exercise `ttnn.experimental.all_reduce_async` — the async variant that accepts explicit `GlobalSemaphore` handles and pre-allocated intermediate tensor pools — not `ttnn.all_reduce` (the synchronous non-async entry point that passes `std::nullopt` for all semaphore args). The inline parenthetical "(the async variant with explicit `GlobalSemaphore` handles and a pre-allocated `tt_intermediate_tensor` pool)" is present but appears only as a subordinate clause after the key claim. An implementer reading the section to confirm that `ttnn.all_reduce` itself is trace-safe will absorb the headline framing — "evidence … CCL-class operations can be traced" — and move on without recognising that the gap between the async and synchronous variants is precisely the question the guide is answering. The async variant's explicit pre-allocation contract is managed by the test harness; the synchronous variant's implicit intermediate allocation is the risk being assessed. Presenting tests of one as evidence for the other is a category (c) misrepresentation. The section should either: (a) retitle to "Evidence from the async variant — analogous pattern, not direct validation" and add a sentence noting that no existing test directly traces `ttnn.all_reduce` (non-async) on T3K, or (b) be moved to a supporting context note with an explicit caveat that direct trace validation of `ttnn.all_reduce` remains an open test gap.

# B Review — Final Pass — Pass 5

1. **`reduce_scatter_all_gather_path.md` and `q3_requirements_and_limitations.md` name the Python keyword argument as `optional_output_tensor`, but `integration_checklist.md`'s own code example uses `output_tensor=` — an implementer following the text description will pass the wrong keyword and the pre-allocation will silently have no effect.**
   `reduce_scatter_all_gather_path.md` §3 Warning (line 139) states: "pass it as the `optional_output_tensor` argument to `ttnn.reduce_scatter`." The §4 summary table repeats "pass as `optional_output_tensor` to `ttnn.reduce_scatter`." `q3_requirements_and_limitations.md` Requirement 2 (line 40) likewise says "passed via the `optional_output_tensor` argument to `ttnn::reduce_scatter`." However, the only concrete Python implementation in the guide — `integration_checklist.md` lines 108–113 — uses the keyword `output_tensor=scattered_tensor`. An implementer who reads the Ch2/Ch3 text descriptions and writes Python accordingly will call `ttnn.reduce_scatter(..., optional_output_tensor=scattered_tensor)`. If the Python binding exposes only `output_tensor` (not `optional_output_tensor`), this call will either raise a `TypeError` for an unexpected keyword argument or, if the binding silently ignores unknown kwargs, bypass the pre-allocation entirely. Either outcome leaves the `scattered_tensor` dynamically allocated during capture, reproducing the address-instability problem the pre-allocation was meant to solve. The text in Ch2 and Ch3 must use the same keyword name as the code example in Ch4. The correct name should be verified against the Python binding signature in `ttnn/python_api/ttnn/operations/ccl.py` (or equivalent) and all three occurrences of `optional_output_tensor` in the text descriptions updated to match.

# B Review — Final Pass — Pass 6

# B Review — Final Pass — Pass 7

1. **`reduce_scatter_all_gather_path.md` §3 Warning prescribes pre-allocating `scattered_tensor` and passing it as `output_tensor` to `ttnn.reduce_scatter`, but this is architecturally impossible through the `ttnn.all_reduce` API — the guide describes a fix that cannot be applied without bypassing the stated entry point.**
   The §3 Warning (line 139) instructs the implementer to "pre-allocate a persistent output buffer for `scattered_tensor` before `ttnn.begin_trace_capture` is called, and pass it as the `output_tensor` argument to `ttnn.reduce_scatter`." The §4 summary table repeats "pass as `optional_output_tensor` to `ttnn.reduce_scatter`." The code in §1, however, shows the internal call inside `all_reduce_async.cpp` (line 42): `std::nullopt, // optional_output_tensor` — this argument is hardcoded as `std::nullopt` and is not forwarded from any parameter of `ttnn.all_reduce` or `ttnn.all_reduce_async` (cluster-axis overload). There is no public Python or C++ API on `ttnn.all_reduce` that exposes an `output_tensor` argument for the internal `ttnn.reduce_scatter` call. The only way to inject a pre-allocated buffer is to call `ttnn.reduce_scatter` and `ttnn.all_gather` directly instead of calling `ttnn.all_reduce`. The guide presents the pre-allocation as a straightforward caller-side precaution while simultaneously showing source code proving it cannot be accomplished through the documented entry point. An implementer following the Warning will attempt to pass `output_tensor=` to `ttnn.all_reduce` (which has no such parameter), or will call `ttnn.reduce_scatter(..., output_tensor=scattered_tensor)` and then `ttnn.all_gather(...)` directly — meaning they are not using `ttnn.all_reduce` at all. The guide must explicitly state that achieving trace-safe pre-allocation of `scattered_tensor` requires replacing the `ttnn.all_reduce` call with a manual `ttnn.reduce_scatter` + `ttnn.all_gather` sequence, or that a source-level patch to `all_reduce_async.cpp` is needed to forward an optional output tensor argument. Leaving the Warning as written leads an implementer to believe they can satisfy the precondition while still calling `ttnn.all_reduce`.

2. **`reduce_scatter_all_gather_path.md` §3 states `scattered_tensor` shape as `[1, 1, 32, hidden_dim / num_devices]`, but the decode shape stated in `q1_trace_compatibility.md` is `[1, 1, 1, out_features]` — an implementer using the §3 shape to pre-allocate the buffer will create a wrong-shaped tensor that causes a runtime shape mismatch.**
   `reduce_scatter_all_gather_path.md` §3 (lines 115–117) gives the concrete shape of `scattered_tensor` as "`[1, 1, 32, hidden_dim / num_devices]`", with 32 as the third dimension and no explanation of where that value comes from. `q1_trace_compatibility.md` line 87 (T3K path-assessment table) states the typical decode shape entering `ttnn.all_reduce` as "`[1, 1, 1, out_features]` after `ttnn.linear`" — sequence length 1, not 32. A `reduce_scatter` along dimension 3 of a `[1, 1, 1, out_features]` tensor produces `[1, 1, 1, out_features / 8]`, not `[1, 1, 32, out_features / 8]`. The `integration_checklist.md` code (lines 97–98) correctly derives the shape by copying `tt_output.shape` and replacing only the last element, which gives the right result regardless of sequence length; but §3's hardcoded shape directly contradicts it. An implementer who reads §3 and manually allocates `ttnn.zeros([1, 1, 32, out_features // 8], ...)` will produce a buffer whose shape does not match the `reduce_scatter` output, causing either a shape mismatch error or silent buffer aliasing depending on how the pre-allocated tensor is injected. The §3 description must either (a) replace the concrete `[1, 1, 32, ...]` with the general form `[1, 1, seq_len, out_features / ring_size]` and note that `seq_len` matches the sequence dimension of the input tensor, or (b) note explicitly that the 32 reflects TILE_LAYOUT height-padding of a seq_len=1 decode tensor and is therefore the padded — not logical — shape.

# B Review — Final Pass — Pass 8

No feedback — guide approved.
